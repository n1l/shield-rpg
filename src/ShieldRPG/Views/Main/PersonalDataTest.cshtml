@model ShieldRPG.Models.PersonalDataRequest

@{
    ViewData["Title"] = "Личные дела агентов";
    Layout = null;
}

<!DOCTYPE html>

<div class="row">
    <div class="col-md-4">
        <form asp-action="PersonalDataRequest">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PersonalDataCode" class="control-label"></label>
                <input asp-for="PersonalDataCode" class="form-control" />
                <span asp-validation-for="PersonalDataCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Запрос" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
