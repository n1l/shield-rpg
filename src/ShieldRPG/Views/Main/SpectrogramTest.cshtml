@model ShieldRPG.Models.SpectrogramRequest

@{
    ViewData["Title"] = "Анализ на токсины";
    Layout = null;
}

<!DOCTYPE html>

<div class="row">
    <div class="col-md-9">
        <form asp-action="SpectrogramRequest">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SpectrogramCode" class="control-label"></label>
                <input asp-for="SpectrogramCode" class="form-control" />
                <span asp-validation-for="SpectrogramCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Запрос" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
