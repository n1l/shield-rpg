@model ShieldRPG.Models.ScanRequest

@{
    ViewData["Title"] = "Cканирование объекта";
    Layout = null;
}

<!DOCTYPE html>

<div class="row">
    <div class="col-md-4">
        <form asp-action="ScanRequest">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ScanCode" class="control-label"></label>
                <input asp-for="ScanCode" class="form-control" />
                <span asp-validation-for="ScanCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Запрос" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
