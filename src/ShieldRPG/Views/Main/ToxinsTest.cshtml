@model ShieldRPG.Models.ToxinsRequest

@{
    ViewData["Title"] = "Анализ на токсины";
    Layout = null;
}

<!DOCTYPE html>

<div class="row">
    <div class="col-md-9">
        <form asp-action="ToxinsRequest">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ToxinsCode" class="control-label"></label>
                <input asp-for="ToxinsCode" class="form-control" />
                <span asp-validation-for="ToxinsCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Запрос" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
